<h1>Posts#index</h1>
<p>Find me in app/views/posts/index.html.erb</p>

<% if logged_in? %>
	<% @posts.each do |f| %>
		<li>
			<%= f.user.name %>
			<%= f.content %>
		</li>
	<% end %>
<% else %>
	<% @posts.each do |f| %>
		<li>
			<%= f.content %>
		</li>
	<% end %>
<% end %>