<h1>Posts#index</h1>
<p>Find me in app/views/posts/index.html.erb</p>

<% if logged_in? %>
	<% @posts.each do |post| %>
		<li>	
			<%= post.user.name %>
			<%= post.content %>
		</li>
	<% end %>
<% else %>
	<% @posts.each do |post| %>
		<li>
			<% post.content %>
		</li>
	<% end %>
<% end %>
